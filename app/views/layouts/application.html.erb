<!DOCTYPE html>
<html>
  <head>
    <title>RailsWatchList</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <div class="navbar navbar-expand-lg navbar-light bg-light px-3 py-2 d-flex justify-content-between align-items-end" >
      <p><%= link_to '🍿', lists_path, class: "navbar-brand"%></p>
      <div>
      <div class="btn btn-outline-primary">
        <%= link_to 'My lists', lists_path, :class => "btn text-primary" %>
      </div>
      <div class="btn btn-outline-primary">
        <%= link_to 'New list', new_list_path, :class => "btn text-primary" %>
      </div>
      </div>
    </div>
    <%= yield %>
  </body>
</html>
